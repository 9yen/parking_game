# AGENTS.md

## 目标

使用 Godot 4.x 和 GDScript 开发 2D 桌面挂机游戏《一起停车吧》。当前实现以 `spec/` 中的 MVP 为准，优先验证停车、计时收益、成长和小窗体验。

## 工作前必读

1. 阅读 `spec/README.md`。
2. 阅读与任务相关的规格文件。
3. 检查现有场景、脚本、测试和未提交改动，避免覆盖用户工作。
4. 如果需求与规格冲突，以用户当前明确指示为准，并同步更新规格。

## 仓库结构

- `spec/`：当前版本目标、产品范围、视觉和机制规格。新增规格时更新 `spec/README.md`。
- `assets/`：游戏资源文件。说明见 `assets/README.md`。
- `docs/`：项目文档。新增文档时必须更新 `docs/index.md`。
- `docs/experience-library/`：已解决问题、复发规则和开发经验。
- `scenes/`：Godot 场景文件；场景负责组合与展示，不承载复杂经济规则。
- `scripts/`：GDScript 业务逻辑、平台适配、调试入口和测试脚本。
- `tests/`：若创建独立测试目录，存放自动化测试、固定样本和测试辅助代码。

## MVP 边界

- 首先实现本地单机与固定 NPC，不提前实现真实异步好友后端、公共匹配或 Steam 市场。
- 不实现自由文字聊天、真实驾驶、碰撞关卡、付费随机宝箱和全球排行榜。
- 远期功能可以预留接口和稳定 ID，但不得为了未来需求过度建设当前系统。

## 代码规范

- 使用 Godot 4.x 支持的 GDScript 语法，优先使用静态类型。
- 文件、变量和函数使用 `snake_case`；`class_name` 使用 `PascalCase`；常量使用 `UPPER_SNAKE_CASE`。
- 函数保持单一职责；避免超大场景脚本、全局可变状态和隐式节点路径依赖。
- 通过信号或明确接口连接 UI 与领域逻辑，禁止 UI 直接修改金币、车辆状态或存档字段。
- 收益、价格、等级、冷却和概率采用数据驱动配置，不把平衡数值散落在场景和脚本中。
- 核心计时使用时间差计算，不依赖逐帧累加。
- 时间来源必须可注入或可替换，测试不得依赖真实等待。
- 随机来源应支持固定种子，保证掉落测试可重复。
- Steam、存档、网络、天气和系统窗口能力放在适配层后面；领域逻辑不得直接调用平台单例。
- 没有 Steam、断网或平台接口失败时，必须退回本地 NPC 模式。
- 所有外部输入均不可信，包括 Steam 名称、系统时间、网络响应和存档内容。

## 状态与存档

- 车辆业务状态必须显式建模，至少区分车库中、停车中和返回中。
- 同一车辆不得同时出现在多个车位或重复结算。
- 存档必须包含格式版本，并提供向前迁移路径；禁止无提示破坏旧存档。
- 金币、经验、掉落和停车结算应具备幂等标识，重复加载或重试不得重复发奖。
- MVP 可以使用本地时间，但涉及真实排行榜、交易或好友共享后必须使用可信服务器时间。
- 不把密钥、Steam Web API 发布者密钥、服务端凭据或真实玩家数据提交到仓库。

## 本地化与内容

- 玩家可见文本必须通过本地化键获取，不得硬编码在场景或业务脚本中。
- MVP 至少维护简体中文和英语文本。
- 网络互动保存事件 ID，不保存某一种语言的最终句子。
- UI 预留较长翻译空间，并处理 Unicode、缺字、截断和特殊字符。
- 车辆和场景使用虚构品牌，不复制现实车标、车型名称或其他游戏资产。

## 测试要求

每个机制变更必须补充或更新自动化测试。核心逻辑至少覆盖：

- 空车位占用与满车场拒绝。
- 同一车辆重复派出保护。
- 主动召回与自动返回。
- 8 小时收益上限及边界时间。
- 车位主人分成不扣减访客收益。
- 离线报告重复读取不重复发奖。
- 车辆外出时禁止出售和改装。
- 存档版本迁移、损坏数据和缺失字段。
- 固定随机种子下的掉落结果。
- 转生重置与保留清单；在功能实现后启用。

优先测试不依赖场景树的纯领域逻辑。需要场景测试时使用最小测试场景。不要通过真实等待数分钟或数小时验证计时。

新增测试框架或第三方插件前先检查项目现状；未经用户同意不要引入重量级依赖。

## 验证与交付

- 修改后运行与风险相称的自动化测试和 Godot 无头验证。
- 检查启动错误、脚本解析错误、资源缺失和本地化缺字。
- 涉及 UI 或视觉时，同时验证普通窗口与紧凑小窗，以及简体中文和英语。
- 涉及时间、收益或存档时，报告验证过的边界案例。
- 无法运行的测试必须说明原因和未验证风险，不能声称已经通过。
- 新增或改变产品行为时同步更新 `spec/`；新增项目文档时同步更新 `docs/index.md`。

## 文档风格

- 说明“为什么”和可验证结果，避免只记录操作步骤。
- 对未决定内容使用“暂定”或“待验证”，不要把猜测写成最终规则。
- 经验库条目应包含现象、根因、修复、回归测试和防复发规则。
